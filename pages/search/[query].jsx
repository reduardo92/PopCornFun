import React from 'react';
import styled from 'styled-components';

const Styled = styled.section``;

const SearchQuery = () => {
  return <Styled className='search--section'>Hello from Search</Styled>;
};

export default SearchQuery;
